import { Container } from '@material-ui/core';
import * as React from 'react';

export const Page: React.FC = () => {
    return (
        <React.Fragment>
            <Container>
                Hello
            </Container>    
        </React.Fragment>
    );
};